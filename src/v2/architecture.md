# Архитектура системы

## 4.1. Общий архитектурный подход

Система строится как модульная клиент-серверная информационная система с выделенными функциональными компонентами: управление записью, управление расписанием, электронная очередь, уведомления, отчётность и аудит. Критичные операции, связанные с созданием записи на слот, выполняются транзакционно, так как основным архитектурным риском является конкурентная запись нескольких пользователей на одно и то же время.

Для уменьшения связности внешние уведомления выполняются асинхронно через очередь сообщений. Недоступность SMS/e-mail/push-шлюза не должна блокировать создание записи. Для сотрудников используется интеграция с SSO, а для заявителей допускается идентификация через контактные данные, ссылку на запись или личный кабинет.

## 4.2. C4 Context

Контекстная диаграмма показывает систему как единый объект и внешние взаимодействия:

- заявитель взаимодействует с публичным интерфейсом записи;
- сотрудники взаимодействуют с интерфейсом сотрудника и администратора;
- терминалы и табло взаимодействуют с API очереди;
- SSO используется для аутентификации сотрудников;
- сервис уведомлений отправляет сообщения заявителям;
- внешний календарь предоставляет или принимает данные расписания;
- система мониторинга получает метрики и логи.

@drawio{https://github.com/Stanislav1275/system-engineering-playbook/blob/main/src/diagrams/equeuee.drawio}

## 4.3. C4 Container

На уровне контейнеров система включает:

| Контейнер | Назначение | Основные связи |
|---|---|---|
| Public Web UI | Интерфейс заявителя | API Gateway |
| Staff/Admin UI | Интерфейс сотрудников, администратора и руководителя | API Gateway, SSO |
| Terminal API | API для терминалов и табло | Queue Service |
| API Gateway / Backend API | Единая точка входа, маршрутизация, RBAC, валидация | Все внутренние сервисы |
| Appointment Service | Создание, отмена, перенос записей | PostgreSQL, Redis, Message Broker |
| Schedule Service | Расписание, услуги, окна, расчёт слотов | PostgreSQL, внешний календарь |
| Queue Service | Талоны, статусы очереди, вызов заявителей | PostgreSQL, табло |
| Notification Worker | Асинхронная отправка уведомлений | Message Broker, Notification Gateway |
| Reporting & Audit Service | Отчёты, аудит, метрики | PostgreSQL, BI |
| Auth Adapter | Проверка токенов и ролей сотрудников | SSO |
| PostgreSQL | Основное транзакционное хранилище | Внутренние сервисы |
| Redis | Кэш, краткоживущие блокировки, idempotency-key | Appointment Service, API Gateway |
| Message Broker | События записи и уведомлений | Appointment Service, Notification Worker |

## 4.4. Ключевые сущности

| Сущность | Описание | Основные поля |
|---|---|---|
| Applicant | Заявитель | applicant_id, full_name, phone, email, created_at |
| Department | Подразделение МФЦ | department_id, name, address, timezone, status |
| Service | Услуга | service_id, name, duration_minutes, description, is_active |
| ServiceWindow | Окно обслуживания | window_id, department_id, name, supported_service_ids, status |
| Schedule | Базовое расписание окна | schedule_id, window_id, day_of_week, start_time, end_time, break_intervals |
| ScheduleException | Исключение расписания | exception_id, window_id, date, start_time, end_time, type, reason |
| Slot | Расчётный временной интервал | slot_id, department_id, service_id, window_id, start_at, end_at, status |
| Appointment | Запись на приём | appointment_id, applicant_id, service_id, department_id, window_id, start_at, end_at, status |
| QueueTicket | Талон электронной очереди | ticket_id, appointment_id, department_id, number, status, issued_at |
| NotificationTask | Задача уведомления | notification_id, appointment_id, channel, status, retry_count, next_retry_at |
| AuditLog | Журнал аудита | audit_id, actor_id, actor_role, action, object_type, object_id, created_at |

## 4.5. Механизм защиты от двойной записи

Консистентность слотов обеспечивается несколькими уровнями защиты:

1. `Idempotency-Key` на операции создания записи, чтобы повторный запрос клиента не создавал дубликат.
2. Краткоживущая блокировка слота в Redis на время выполнения операции.
3. Транзакционное создание записи в PostgreSQL.
4. Уникальное ограничение на активную запись в пределах окна обслуживания и времени начала.
5. Повторная проверка доступности слота непосредственно перед фиксацией записи.

```sql
CREATE UNIQUE INDEX ux_active_appointment_slot
ON appointments (department_id, service_id, window_id, start_at)
WHERE status IN ('pending_confirmation', 'confirmed', 'waiting', 'called', 'in_service');
```

## 4.6. ADR

### ADR-1. Транзакционное создание записи с уникальным ограничением на активный слот

**Контекст:** несколько заявителей могут одновременно выбрать один и тот же свободный слот.

**Решение:** создание записи выполняется в транзакции, а база данных содержит уникальное ограничение на активный слот.

**Альтернативы и причины отклонения:**
- *Только Redis-lock:* при аварийном рестарте Redis блокировка снимается без фиксации записи — возникает окно для двойного бронирования.
- *Только проверка на уровне приложения:* между check и insert при конкурентных запросах существует race condition, который не устраняется без DB-level гарантии.
- *Предварительная генерация всех слотов:* избыточный объём данных, сложность инвалидации при изменении расписания.

**Trade-off:** уникальное ограничение усложняет модель статусов, но даёт надёжную защиту от двойной записи даже при сбое прикладной логики.

### ADR-2. Асинхронная отправка уведомлений через очередь сообщений

**Контекст:** уведомительный шлюз может быть временно недоступен.

**Решение:** после изменения записи система публикует событие, а Notification Worker отправляет уведомления асинхронно.

**Альтернативы и причины отклонения:**
- *Синхронная отправка в рамках запроса:* недоступность SMS/push-шлюза блокирует создание записи — нарушает NFR-4 (доступность 99,5%).
- *Периодический batch без событий:* задержка уведомления непредсказуема, нарушает NFR-11 (≤10 сек от события до постановки в очередь).

**Trade-off:** пользователь может получить уведомление с задержкой, но основная операция записи не блокируется внешним сервисом.

### ADR-3. Разделение Schedule Service и Appointment Service

**Контекст:** расписание определяет потенциальную доступность, а запись фиксирует фактическую занятость.

**Решение:** выделить отдельные компоненты для расчёта слотов и управления записями.

**Альтернативы и причины отклонения:**
- *Единый сервис расписаний и записей:* изменение алгоритма расчёта слотов требует деплоя компонента, управляющего активными записями — высокий риск регрессии и нарушение single responsibility.

**Trade-off:** появляется межсервисное взаимодействие, но упрощается ответственность компонентов и независимое тестирование.

### ADR-4. Использование SSO для сотрудников

**Контекст:** сотрудники организации уже имеют корпоративные учётные записи и роли.

**Решение:** использовать корпоративный SSO для входа сотрудников и проверки ролей.

**Альтернативы и причины отклонения:**
- *Локальные логины и пароли:* требует собственного управления паролями, ротацией, MFA — дублирует функции корпоративного IdP и создаёт второй контур доступа вне контроля ИБ.

**Trade-off:** система зависит от доступности SSO, но снижает риск разрозненного управления учётными записями. Деградация при недоступности SSO смягчается кэшированием активных токенов (ADR-4 → допущение 1.6.3).

### ADR-5. Ведение журнала аудита действий сотрудников

**Контекст:** сотрудники работают с персональными данными и изменяют статусы обслуживания.

**Решение:** фиксировать действия сотрудников в AuditLog.

**Альтернативы и причины отклонения:**
- *Только технические логи приложения:* не содержат actor_id и бизнес-контекст действия — невозможно ответить на вопрос «кто и когда просматривал персональные данные» без ручного парсинга логов.

**Trade-off:** увеличивается объём хранимых данных, но обеспечивается соответствие NFR-7 и расследуемость инцидентов с персональными данными.
